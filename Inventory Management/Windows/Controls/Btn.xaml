<UserControl x:Class="Inventory_Management.Windows.Controls.Btn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border CornerRadius="3" BorderThickness="1" BorderBrush="Transparent">
        <Border.Effect>
            <DropShadowEffect Direction="270" Color="Gray" ShadowDepth="1" />
        </Border.Effect>
        <StackPanel Background="{Binding Backcolor}" ClipToBounds="True" Orientation="Horizontal">
            <Image Name="img_icon" Source="{Binding ImgSource}" Margin="10, 0, 0, 0" Width="30" Height="30" />
            <TextBlock Name="txt_text" Foreground="White" FontSize="15" Text="{Binding BtnText}" Padding="10, 15, 15, 15" />
        </StackPanel>
    </Border>
</UserControl>
